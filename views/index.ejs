<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="vaultIcon.png" type="image/x-icon" />
    <title>VirtueVault</title>
  </head>
  <body>
    <ul>
      <% messages.map((message) => { %>
      <li id="<%= message.id %>">
        <p><%= message.text %></p>
        <p><%= message.user %></p>
        <time><%= message.added %></time>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
