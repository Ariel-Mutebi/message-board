<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="vaultIcon.png" type="image/x-icon" />
    <title>VirtueVault</title>
  </head>
  <body>
    <ul>
      <% const orderedMessages = messages.sort((a, b) => new Date(a.added) - new
      Date(b.added)); %> <% orderedMessages.map((message) => { %>
      <li>
        <p><%= message.user %></p>
        <p><%= message.text %></p>
        <p><%= message.added %></p>
        <a href="/<%= message.id %>">Details</a>
      </li>
      <% }) %>
    </ul>
    <a href="/new">Create virtue</a>
  </body>
</html>
